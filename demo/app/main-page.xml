<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">
  <StackLayout>
    <Label text="Watch the magic!" class="title"/>
    <Button text="{{ openUrlButtonText }}" tap="{{ onOpenTap }}" textWrap="true" />
	<ios>
      <Button text="{{ openCloseUrlButtonText }}" tap="{{ onOpenCloseTap }}" textWrap="true" />
	</ios>
  </StackLayout>
</Page>
